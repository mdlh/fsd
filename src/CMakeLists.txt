SET(PUBLIC_INCLUDE ${PROJECT_SOURCE_DIR}/include)
SET(PUBLIC_HEADERS ${PUBLIC_INCLUDE}/fsd)
INCLUDE_DIRECTORIES(${PUBLIC_INCLUDE})
SET(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -pedantic -Wall -Werror")

ADD_SUBDIRECTORY(drm)
