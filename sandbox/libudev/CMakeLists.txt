#FIND_PACKAGE(libudev)
include (FindPkgConfig)
pkg_check_modules(LIBUDEV "libudev")
IF ( ${LIBUDEV_FOUND} )
    ADD_EXECUTABLE(udev_list udev.c)
    TARGET_LINK_LIBRARIES(udev_list ${LIBUDEV_LIBRARIES})
ELSE ( ${LIBUDEV_FOUND} )
  MESSAGE (WARNING "Libudev not found")
ENDIF ( ${LIBUDEV_FOUND} )
